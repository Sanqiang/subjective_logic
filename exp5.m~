m = [1 1;1 0]
print_triple(generate_triple(m));

m = generate_matrix(m,1,0.5,1,0,0,0)
print_triple(generate_triple(m));

m = generate_matrix(m,1,0.5,0,1,0,0)
print_triple(generate_triple(m));

m = generate_matrix(m,1,0.5,0,1,0,0)
print_triple(generate_triple(m));

m = generate_matrix(m,1,1.0,0,1,0,0)
print_triple(generate_triple(m));

m = generate_matrix(m,0,0.5,0,0,1,0)
print_triple(generate_triple(m));

